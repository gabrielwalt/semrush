# PROJECT-PLAN.md — Active Tasks

**Pick the first `🔲 Open` task, implement it fully, mark it `✅ Done`, then move to the next.**

---

## How to work this list

**Required skills:** Load `executing-plan-tasks` before starting any task.

**Execution protocol:**
1. Read the task. Read the files it references before touching anything.
2. Implement the change.
3. Verify after coding: reload preview, confirm no regressions.
4. Mark the task `✅ Done` and update `PROJECT-STATUS.md`.
5. **After 2 failed fix attempts: STOP.** Ask the user.

---

## Safeguards for cleanup work

Before starting Phase 1:
1. Take a full-page screenshot at 1920px and save as `cleanup-baseline.png`
2. After each task group, re-screenshot and diff against baseline
3. If any unintended visual change → revert immediately

---

## Phase 1 — Dead Code Removal

### C01 — ✅ Done — Remove unused `scripts/glass.js`

**Files:** Delete `scripts/glass.js`
**Why:** Exported but never imported anywhere. The `glass-surface` class is applied directly in block JS.
**Verify:** Grep for `glass.js` — zero imports. Page loads without error.

---

### C02 — ✅ Done — Remove dead CSS rules (4 selectors)

**Files:** `blocks/testimonials/testimonials.css`, `blocks/header/header.css`, `styles/styles.css`
**What to remove:**
- `.quote-card .quote-logo img` — class `quote-logo` never generated by JS
- `header nav .nav-sections .nav-mega-promo-meta` — class never generated by JS
- `main .section.light, main .section.highlight` — no content uses these section styles
- `/* backward-compat aliases */` block in `:root` (lines ~85-94) — grep first to confirm none are used in any CSS file
**Verify:** Grep each class/variable name across all files to confirm zero usage.

---

### C03 — ✅ Done — Remove unused block directories

**Files:** Delete these directories entirely:
- `blocks/hero/` — auto-build is always bypassed (homepage has `insights-widget`); 5-line no-op JS + 38-line unused CSS
- `blocks/logo-marquee/` — no content references this variant
- `blocks/cards/` — boilerplate, never used (project uses `cards-icon` and `cards-awards`)
- `blocks/columns/` — boilerplate, never used (project uses `columns-stats`)
**Pre-check:** Grep for each block name (`class="hero"`, `class="logo-marquee"`, `class="cards"`, `class="columns"`) in ALL content files including nav/footer/enterprise. Only delete if zero matches.
**Verify:** Page loads. No 404s for block JS/CSS in console.

---

### C04 — ✅ Done — Assess and remove footer sub-block directories

**Files:** `blocks/footer-cta/`, `blocks/footer-links/`, `blocks/footer-bottom/`
**Context:** These were used when the footer had 3 separate sections. Now all 3 blocks are in a single section, and all styling is in `blocks/footer/footer.css`. The sub-block JS files are 3-line no-ops or 15-line mobile accordions.
**Pre-check:** Load the page. Check if EDS applies `-cta`, `-links`, `-bottom` as block classes in the DOM. If yes, EDS will try to load their CSS/JS — the directories must stay (or the styling must move to footer.css). If the footer.css already covers everything, the sub-block CSS is purely dead weight.
**Action:** If sub-block CSS has unique rules not in footer.css, merge them first. Then delete.
**Verify:** Footer renders identically.

---

### C05 — ✅ Done — Assess redirect blocks for removal

**Files:** `blocks/solutions-slider/`, `blocks/hero-video/`, `blocks/promo-cards/`
**Context:** These exist because remote AEM content may reference old block names. Each is a 1-line CSS import + 3-line JS re-export.
**Pre-check:** Fetch the live served HTML from `https://aem-merged-20260513--semrush--gabrielwalt.aem.page/index.plain.html` and grep for `solutions-slider`, `hero-video`, `promo-cards`. If the content uses the NEW names (`carousel-slider`, `video`, `video-card`), the redirects are dead.
**Verify:** Page loads without block-not-found errors in console.

---

## Phase 2 — Code Consolidation

### C06 — ✅ Done — Extract shared `getVideoSources()` utility

**Files:** Create `scripts/video-utils.js`. Update `blocks/video/video.js`, `blocks/video-card/video-card.js`, `blocks/video-card-feature/video-card-feature.js` to import from it.
**Why:** 3 near-identical copies of the same 15-line function.
**Verify:** Hero video, both video-cards, and video-card-feature all play correctly.

---

### C07 — 🔲 Open — Merge near-identical card blocks

**Files:** `blocks/cards-icon/cards-icon.js`, `blocks/cards-awards/cards-awards.js`
**Why:** Both are 21 lines following the exact same pattern (ul/li restructure + optimized pictures), differing only in class prefix.
**Action:** Create shared card decorator in a utility. Both blocks call it with their prefix.
**Verify:** Both blocks render correctly on Semrush One / Enterprise pages.

---

### C08 — ✅ Done — Merge `columns-stats.js` into `columns.js` pattern

**Files:** `blocks/columns-stats/columns-stats.js`, `blocks/columns/columns.js`
**Why:** 18 lines each, near-identical. If columns.js is deleted (C03), columns-stats.js stands alone — just verify it's clean.
**Verify:** columns-stats renders correctly.

---

### C09 — 🔲 Open — Consolidate duplicate import parsers

**Files:** `tools/importer/parsers/promo-cards-semrush-one.js`, `tools/importer/parsers/promo-cards-enterprise.js`
**Why:** 76 lines each, identical structure, differ only in block name string and CTA style detection.
**Action:** Create one parameterized parser. Both files become thin wrappers.
**Verify:** Re-bundle and run import. Output matches.

---

## Phase 3 — Spacing & Layout Simplification

### C10 — ✅ Done — Document the spacing system

**Files:** `styles/styles.css`
**Action:** Rewrite the spacing system comment block to be a complete reference:
- Section padding: 60px desktop / 30px mobile
- Block margin: 60px desktop / 30px mobile (via `* + *`)
- First/last child: zero margin
- `section-flush`: zero padding variant
- Carousel left-margin `max()` formula explanation
**Verify:** No code changes, only comments.

---

### C11 — ✅ Done — Audit and remove any remaining block-level padding overrides

**Files:** All block CSS files
**Action:** Search for `padding-top`, `padding-bottom` on section or wrapper selectors in block CSS. If any duplicate the global `--section-padding` rule, remove them.
**Verify:** Section spacing unchanged.

---

## Phase 4 — Readability & Documentation

### C12 — ✅ Done — Add file-level comments to all block CSS and JS files

**Files:** All 29 block CSS files, all 29 block JS files
**Action:** Add a 1-line comment at the top: block name + what it does + which pages use it. Example: `/* Carousel Slider — expandable solutions cards + resource card strip (homepage) */`
**Verify:** No functional changes.

---

### C13 — ✅ Done — Fix Lazzer font-weight 500

**Files:** `styles/fonts.css`
**Why:** Body uses `font-weight: 500` but only weights 400/600/700 are loaded. Browsers synthesize 500 from 400, causing slightly different rendering.
**Action:** Either add a `@font-face` for Lazzer 500 or change `body { font-weight: 400 }`.
**Verify:** Body text renders without faux-bold artifacts.

---

### C14 — ✅ Done — Simplify `scripts/scripts.js` — remove dead hero auto-build

**Files:** `scripts/scripts.js`
**Why:** `buildHeroBlock()` is never triggered on any current page (bypassed when insights-widget or video blocks exist). It adds complexity to `buildAutoBlocks()` for no benefit.
**Action:** Remove `buildHeroBlock()` and its call from `buildAutoBlocks()`. If C03 deletes `blocks/hero/`, this function has no target block anyway.
**Verify:** Homepage and all other pages load correctly.

---

### C15 — ✅ Done — Create authoring reference for section styles and spacing

**Files:** New file or section in PROJECT-BLOCKS.md
**Action:** Document all available section styles (`section-centered`, `section-dark`, `section-ai-visibility`, `section-flush`) and block spacing variants (`spacing-top-none/small/large`, `spacing-bottom-none/small/large`) with usage examples.
**Verify:** Documentation only.

---

## Phase 5 — Quality & Scalability (from audit)

### Q01 — ✅ Done — Centralize hardcoded colors into CSS variables

**Priority:** P2  
**Risk:** Low (visual-only, tokenize one at a time)  
**Files:** `styles/styles.css` (add variables), then all block CSS files  
**Why:** 5+ blocks use `rgb(108 110 121)` (muted grey), `rgb(230 230 230)` (border grey), `rgb(220 238 235)` / `#dceeeb` (brand teal), and `#fff` directly. These should be `--color-muted`, `--color-border`, `--color-teal`, and `var(--background-color)`.  
**Action:** Add 3 new CSS variables to `:root`. Find-and-replace across all block CSS files.  
**Verify:** Visual output identical. Grep confirms no remaining raw instances.

---

### Q02 — ✅ Done — Fix accessibility: tabs block ARIA pattern

**Priority:** P1 (critical a11y)  
**Risk:** Low  
**Files:** `blocks/tabs/tabs.js`, `blocks/tabs/tabs.css`  
**Why:** Tab buttons lack `id`, panels lack `aria-labelledby`, no arrow key navigation between tabs. Breaks WAI-ARIA tabs pattern.  
**Action:** Add `id` to each tab button, `aria-labelledby` to each panel, arrow key handler for Left/Right navigation, `tabindex="-1"` on inactive tabs.  
**Verify:** Screen reader announces tab names. Arrow keys cycle tabs.

---

### Q03 — ✅ Done — Fix accessibility: case-study play button

**Priority:** P1 (critical a11y)  
**Risk:** Low  
**Files:** `blocks/case-study/case-study.js`  
**Why:** Play button is a `<div>` — not focusable, not keyboard-operable.  
**Action:** Change to `<button>` or add `role="button"`, `tabindex="0"`, and `keydown` handler for Enter/Space.  
**Verify:** Can Tab to play button, activate with Enter.

---

### Q04 — ✅ Done — Fix accessibility: stats-facts and footer-links keyboard support

**Priority:** P2  
**Risk:** Low  
**Files:** `blocks/stats-facts/stats-facts.js`, `blocks/footer-links/footer-links.js`  
**Why:** Stat rows and footer link headings are clickable but not keyboard-accessible (no `tabindex`, `role`, or keydown handlers).  
**Action:** Add `role="button"`, `tabindex="0"`, and keydown handlers (Enter/Space) to both.  
**Verify:** Can Tab and activate with keyboard.

---

### Q05 — ✅ Done — Add defensive guards to content model assumptions

**Priority:** P2  
**Risk:** Low  
**Files:** `blocks/testimonials/testimonials.js`, `blocks/columns-stats/columns-stats.js`, `blocks/stats-visibility/stats-visibility.js`  
**Why:** These blocks index into `rows[0]`, `rows[1]`, `rows[2]` or `paragraphs[0]`, `paragraphs[1]` with minimal validation. An author adding or removing a row would silently break the block.  
**Action:** Add `if (!rows[0]) return;` guards and optional-chaining fallbacks. Log a warning if the expected row count doesn't match.  
**Verify:** Block renders with correct content. Deliberately test with fewer rows to confirm graceful degradation.

---

### Q06 — ✅ Done — Remove unused Ubuntu font and fix double font loading

**Priority:** P2 (performance)  
**Risk:** Low  
**Files:** `head.html`, `styles/fonts.css`  
**Why:** `head.html` loads Google Fonts CSS2 with both Inter and Ubuntu. Ubuntu is never referenced. `fonts.css` re-declares Inter `@font-face` separately. This causes duplicate font declarations and a wasted Ubuntu download.  
**Action:** Remove the Google Fonts `<link>` from `head.html` entirely (fonts.css handles Inter). Or if the Google Fonts link is needed for other pages, at least remove Ubuntu from the family list.  
**Verify:** Fonts still load correctly. No FOUT.

---

### Q07 — ✅ Done — Add `try/catch` around fragment loading in header and footer

**Priority:** P2  
**Risk:** Low  
**Files:** `blocks/header/header.js`, `blocks/footer/footer.js`  
**Why:** `await loadFragment(path)` can throw on network errors. Neither header nor footer wraps the call in try/catch — a failed fragment load would prevent the entire block from decorating.  
**Action:** Wrap in try/catch with `console.error` fallback.  
**Verify:** Simulate network failure — block degrades gracefully instead of throwing.

---

### Q08 — 🔲 Open — Add focus-visible styles for interactive block elements

**Priority:** P3  
**Risk:** Low  
**Files:** `blocks/carousel-slider/carousel-slider.css`, `blocks/tabs/tabs.css`, `blocks/stats-facts/stats-facts.css`, `blocks/insights-widget/insights-widget.css`  
**Why:** Buttons and interactive elements inside blocks rely on browser default focus rings. Custom `:focus-visible` styles would match the brand's visual language.  
**Action:** Add outline or box-shadow focus styles for nav buttons, tab buttons, stat rows, expand buttons, country picker.  
**Verify:** Tab through the page — focus ring is visible on all interactive elements.

---

## Completed work (archive)

| ID Range | Area | Count |
|----------|------|-------|
| P01–P05 | Merge fixes | 5 |
| M01–M08 | Skills library | 8 |
| T01–T18 | Homepage blocks & layout | 18 |
| E01–E04 | Enterprise blocks | 4 |
| V01–V02 | Asset verification | 2 |
| H01–H26 | Homepage visual refinement | 26 |
| R01–R06 | Second-pass refinement | 6 |
| **Total** | | **69 tasks completed** |

---

## Phase 6 — Skills Library Improvement

### S01 — ✅ Done — Fix `vertical-spacing-system` skill (WRONG)

**Priority:** P1 (skill gives incorrect advice)  
**Files:** `skills/vertical-spacing-system/SKILL.md`  
**Why:** The skill claims block spacing uses `[class$="-wrapper"] + [class$="-wrapper"]` selectors and warns against `* + *`. The actual code at `styles.css:425` uses `main > .section > * + *`. The skill's central claim is inverted from reality.  
**Action:** Rewrite to reflect current spacing system: `--section-padding: 60px/30px`, `--block-padding: 60px/30px`, `* + *` selector, first/last child zeroed, `section-flush` variant.

---

### S02 — ✅ Done — Fix `carousel-pattern-eds` skill (incomplete)

**Priority:** P2  
**Files:** `skills/carousel-pattern-eds/SKILL.md`  
**Why:** Missing the `max()` left-margin formula that is the actual implementation at `carousel-slider.css:44`. The skill says `margin-left: var(--container-padding)` which is a simplification.  
**Action:** Add the `max(var(--container-padding), calc((100vw - var(--container-max-width)) / 2))` formula with explanation of why it ensures content-area alignment at all viewport widths.

---

### S03 — ✅ Done — Merge `mobile-nav-click-handling` into `nav-header-eds`

**Priority:** P3  
**Files:** Delete `skills/mobile-nav-click-handling/`, update `skills/nav-header-eds/SKILL.md`, update `skills/README.md`  
**Why:** The closeOnFocusLost mobile bug is documented in both. The click delegation recipe is the only unique content in mobile-nav-click-handling.  
**Action:** Move the click delegation recipe into nav-header-eds as a subsection. Delete the standalone skill. Update README.

---

### S04 — ✅ Done — Merge `block-rename-in-eds` into `block-rename-checklist`

**Priority:** P3  
**Files:** Delete `skills/block-rename-in-eds/`, update `skills/block-rename-checklist/SKILL.md`, update `skills/README.md`  
**Why:** Both cover block renaming. One is the checklist, the other is the remote-content redirect workaround. Should be a single skill.  
**Action:** Add the redirect pattern as a "Remote content" subsection in block-rename-checklist. Delete the standalone skill. Update README.

---

### S05 — ✅ Done — Trim oversized skills

**Priority:** P3  
**Files:** `skills/writing-plan-tasks/SKILL.md` (133→85 lines), `skills/executing-plan-tasks/SKILL.md` (79→63 lines)  
**Why:** Both exceeded the ~40-line target due to duplicated Gap/Enhancement templates.  
**Action:** Consolidated the per-type sections into shared structure; `writing-plan-tasks` now owns the Gap-vs-Enhancement definition, `executing-plan-tasks` references it.

---

### S08 — ✅ Done — Skill library audit (39 → 35 skills)

**Priority:** P2  
**Why:** Library had grown to 39 skills with duplication and low-value entries diluting context.  
**Action:** Scored every skill against Anthropic's skill-authoring best practices (non-obviousness, EDS-specificity, recipe-ness, uniqueness, trigger quality). Merged `measure-first` + `responsive-verification` → `measure-then-implement`; merged `max-width-container-pattern` → `full-width-escape-hatch`; cut `eds-troubleshooting` (docs-search folded into `eds-dom-structure`). Fixed factually-wrong `* + *` advice in `css-specificity-eds`. De-duplicated the sticky/overflow, CTA-button, and run-bulk-import notes to single owners + cross-refs. Updated `skills/README.md` index.

---

### S06 — ✅ Done — Remove `.full-width` duplication from `eds-code-conventions`

**Priority:** P3  
**Files:** `skills/eds-code-conventions/SKILL.md`  
**Why:** Lines 14-20 duplicate the `.full-width` CSS recipe from `full-width-escape-hatch`. Replace with a `See also:` reference.

---

### S07 — ✅ Done — Add missing knowledge to existing skills

**Priority:** P2  
**Files:** Multiple skills  
**Action:**
- `eds-dom-structure`: Add the inner block table DOM (`.block > div (rows) > div (row) > div (cells)`) for multi-cell rows
- `plain-html-format` or `eds-content-modeling`: Add note about footer single-section content model (no `<hr>` between blocks)
- `eds-migration-process`: Add prominent warning about import script overwriting content files
- `nav-header-eds`: Add note that `overflow: hidden` on html/body breaks `position: sticky` — use `overflow-x: clip` instead

---

## Future priorities (not yet planned)

- **Semrush One page** — re-import content, verify blocks
- **PageSpeed 100** — performance validation
- **Accessibility WCAG 2.1 AA** — audit and fix
- **Bulk import** — additional pages (Pricing, SEO, Features)
- **Mobile polish** — responsive refinement pass at <768px and 768-1023px
